{
  "name": "cm-pdf-viewer-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter demo dev",
    "build": "pnpm --filter @cannyminds/pdf-viewer build",
    "lint": "pnpm --recursive lint",
    "test": "pnpm --recursive test"
  }
}