// // import { PdfiumEngine } from "@embedpdf/pdfium"

// export default async function download(pdfBuffer: ArrayBuffer) {
//   // 1. Create engine instance
//   const engine = await PdfiumEngine()

//   const doc = await engine.openDocument(pdfBuffer)

//   // 3. Access pages
//   const pageCount = doc.pageCount
//   // Page count available

//   // 6. Cleanup
//   await engine.closeDocument(doc)
// }

